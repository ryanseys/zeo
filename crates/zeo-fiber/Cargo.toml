[package]
name = "zeo-fiber"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# Pinned to the 0.3.x line: the safety argument in lib.rs relies on an
# implementation invariant of corosensei's Yielder representation (a fixed
# slot on the coroutine's own stack) that is structural in 0.3.x but not a
# documented API guarantee -- re-verify before any major-version bump.
corosensei.workspace = true

[lints]
workspace = true
