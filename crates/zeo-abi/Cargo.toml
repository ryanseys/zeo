[package]
name = "zeo-abi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Shared ABI: the zero-dependency leaf crate both the zeo compiler and the zeo-rt runtime agree on"
documentation = "https://docs.rs/zeo-abi"
readme = "README.md"
# What the published `.crate` carries: the source, the two licences and the
# README. Tests and fixtures are for the repository, not for a consumer.
include = ["/src", "/README.md", "/LICENSE-MIT", "/LICENSE-APACHE"]

[dependencies]
# Deliberately empty: this crate is the zero-dependency leaf both `zeo`
# (the compiler) and `zeo-rt` (the runtime) share -- see src/lib.rs.

[lints]
workspace = true
