[package]
name = "spinel-abi"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
# Deliberately empty: this crate is the zero-dependency leaf both `spinelc`
# (the compiler) and `spinel-rt` (the runtime) share -- see src/lib.rs.
