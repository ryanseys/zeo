# The Ruby half of `json`. Its native half is the statically linked `ext-json`
# module in zeo-rt, joined to this gem by name.
Gem::Specification.new do |s|
  s.name = "json"
  s.version = "2.18.0"
  s.summary = "JSON's Ruby-level surface: the error hierarchy."
  s.require_paths = ["lib"]
end
