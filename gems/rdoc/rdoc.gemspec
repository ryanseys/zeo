# Vendored from rdoc 8.0.0 (see gems/UPSTREAM.md).
Gem::Specification.new do |s|
  s.name = "rdoc"
  s.version = "8.0.0"
  s.summary = "RDoc produces HTML and command-line documentation for Ruby projects."
  s.require_paths = ["lib"]
end
