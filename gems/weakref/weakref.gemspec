# Vendored from ruby 4.0.6's bundled weakref 0.1.4 (see gems/UPSTREAM.md).
Gem::Specification.new do |s|
  s.name = "weakref"
  s.version = "0.1.4"
  s.summary = "Allows a referenced object to be garbage-collected."
  s.require_paths = ["lib"]
end
