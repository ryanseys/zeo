# Vendored from ruby 4.0.5's bundled timeout 0.6.1 (see gems/UPSTREAM.md).
Gem::Specification.new do |s|
  s.name = "timeout"
  s.version = "0.6.1"
  s.summary = "Timeout.timeout: interrupt a slow block."
  s.require_paths = ["lib"]
end
