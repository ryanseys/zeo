# Vendored from ruby 4.0.5's bundled ostruct 0.6.3 (see gems/UPSTREAM.md).
Gem::Specification.new do |s|
  s.name = "ostruct"
  s.version = "0.6.3"
  s.summary = "OpenStruct: a data object accepting arbitrary attributes."
  s.require_paths = ["lib"]
end
