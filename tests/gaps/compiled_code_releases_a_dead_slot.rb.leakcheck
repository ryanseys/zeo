This gap is ABOUT the compiled-ownership ledger, so it has to run under it.
Without `ZEO_RT_LEAKCHECK=1` the program prints the right answer and the bad
release goes unnoticed, which is the whole problem.
