# The cycle this program builds on purpose, and the census the
# `ZEO_RT_GCCHECK=1` leg gates against. A cycle alive at exit is not a
# defect: it is a ring the program never broke. What the leg gates is a
# CHANGE to the line below.
#
# The ANCHORS corpus, loaded three ways each. Five of its documents alias the container they sit in -- a sequence, a mapping, the nested `a`/`b` pair, `a: &x [*x]` and the self merge key -- so each successful load revives a ring.

cycle leak: 16 objects (Hash x12, Array x4)
