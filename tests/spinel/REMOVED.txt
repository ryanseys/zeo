# Spinel corpus tests deliberately NOT vendored into zeo's corpus (see
# UPSTREAM.md "Removed tests"). scripts/import-spinel-corpus.sh skips these.
compile_time_attribute_singular
compile_time_attribute_wrapped_record
compile_time_attributes
compile_time_define_method_predicates
native_binding_poc
regexp_inline_options

# Spinel-native FFI DSL with no `ffi`-gem analog at all, so a port would assert
# nothing: ffi_buffer_bounds_ok pins a COMPILE-TIME bounds refusal on
# `ffi_buffer` accessors (the gem has no compile-time buffer declaration),
# ffi_const_case_when pins spinel's private `ffi_const` value table against the
# constant table, and ffi_source embeds a C fragment into the generated
# translation unit. Their siblings ffi_read_write_widths and
# ffi_type_list_forms DID port, onto FFI::MemoryPointer and FFI::Library.
ffi_buffer_bounds_ok
ffi_const_case_when
ffi_source
